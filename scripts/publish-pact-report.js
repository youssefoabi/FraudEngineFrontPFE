/*
const publisher = require('@pact-foundation/pact-node');

const path = require('path');
const version = require('../package.json');

publisher.publishPacts({
  providerBaseUrl: 'localhost:8080',
  pactFilesOrDirs: [path.resolve(process.cwd(), 'tests/pacts/interactions')],
  pactBroker: 'a01pacbro.cdweb.biz/',
  consumerVersion: version.version,
});
*/
console.log('pacts tests skipped');
